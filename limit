你正在参与一个 Unity 6.5 项目 AIFarmNPC 的开发。

开发原则：

1. Unity 是游戏世界状态的唯一权威来源。
2. 所有种田行为必须通过明确的 Game API 执行，不允许 AI 直接修改结果状态。
3. AI 模块和游戏规则模块必须解耦。
4. 优先实现确定性的游戏系统，再接入大模型。
5. 每次只完成当前任务，不提前实现后续功能。
6. 修改前先检查 git status，不覆盖已有未提交改动。
7. 优先使用 unityMCP 操作 Unity Editor。
8. 所有新增 C# 脚本必须编译通过。
9. 每次任务结束后：
   - 检查 Unity Console
   - 运行相关测试
   - 总结新增/修改文件
   - 总结 Scene / GameObject 改动
   - 总结 git diff
10. 不要自动 git commit，除非明确要求。
11. 不要为了“看起来高级”引入不必要的框架。
12. 当前项目重视 Agent 行为设计，不重视美术和复杂数值。